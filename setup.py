from setuptools import find_packages, setup

setup(
    name='neuralodes',
    packages=find_packages(),
    version='0.0.1',
    description='Neural ODEs',
    author='',
    license='',
    entry_points={
        'console_scripts': [
        ]
    }
)
